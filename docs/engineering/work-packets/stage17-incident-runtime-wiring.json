{
  "plan_item": "Stage 17 / production incident runtime wiring",
  "intent": "Wire the verified durable Telegram notifier, durable ADGO incident runtime and callback ingress into one application lifecycle without conflating the existing incident read-model with the orchestration control plane or exposing a new public transport route in the same slice.",
  "status_before": "PARTIAL",
  "risk_class": "CRITICAL",
  "invariants": [
    "The existing internal/incidents service remains the read-model/correlation service; the ADGO incident service is a distinct control-plane component.",
    "The production ADGO incident runtime is created only when the durable Telegram notifier can be constructed from the migrated SQLite database, current user store and Telegram client; medium/high incidents never run with a nil notifier.",
    "The ADGO production root is deterministic, local to the Sentinel data directory and independent of SQLite files so process restart addresses the same durable executions.",
    "Callback ingress, when callbacks are enabled, references the exact same ADGO incident service instance used by production orchestration and the same current user/audit stores used by the application.",
    "Callback security being enabled while the production incident runtime is unavailable fails startup closed rather than exposing an unusable or partially-authorized callback authority.",
    "The long-lived ADGO Serve loop is owned by the application run context; context cancellation is normal shutdown, while an unexpected Serve failure is surfaced in health/log state.",
    "Application shutdown cancels and joins the incident orchestration service before closing its durable runtime and before the shared SQLite database is closed.",
    "No Telegram token, callback signing key material or other secret is copied into ADGO workflow data, audit details or health messages.",
    "This slice exposes an explicit application trigger entrypoint but does not infer domainincident.Trigger from arbitrary raw events; event-to-trigger policy remains a separate reviewed boundary.",
    "Concurrency tests use deterministic synchronization rather than sleep-based correctness."
  ],
  "contracts": [
    "internal/orchestration/incident Service, Dependencies and CallbackIngress",
    "internal/telegram DurableNotifier and NotificationDeliveryStore",
    "internal/app lifecycle and CallbackSecurity",
    "internal/incidents read-model/correlation service",
    "github.com/Homiakus/axiom/adgo Production lifecycle",
    "docs/ARCHITECTURE.md",
    "docs/testing/MUTATION_TESTING.md"
  ],
  "code_surfaces": [
    "internal/app/app.go",
    "internal/app/incident_runtime.go",
    "internal/app/incident_runtime_test.go",
    "internal/orchestration/incident",
    "internal/telegram/notifier_test.go",
    "docs/engineering/ACTIVE_WORK_PACKET.json"
  ],
  "required_gates": [
    "static",
    "unit",
    "property",
    "fuzz",
    "race",
    "contract",
    "integration",
    "fault",
    "mutation-critical",
    "replay",
    "security"
  ],
  "acceptance_evidence": [
    "Telegram-disabled startup does not create an incident orchestration runtime unless a supported non-Telegram notifier is explicitly supplied in tests.",
    "Telegram-enabled startup constructs DurableNotifier with PairingStore, UserStore and NotificationDeliveryStore over the migrated database and opens the ADGO runtime at the deterministic data root.",
    "Callback-enabled startup fails closed unless the same production incident workflow is available, and successful wiring binds CallbackIngress to that exact workflow instance plus current users/audit/security authority.",
    "Normal application cancellation joins the Serve loop without a false degraded health state; injected unexpected Serve failure is surfaced.",
    "Close ordering tests prove orchestration is stopped and closed before shared database teardown.",
    "Explicit application StartIncident accepts a valid domain trigger, is deterministic on redelivery and rejects calls when orchestration is unavailable.",
    "The notifier concurrency regression contains no timing sleep used to establish correctness.",
    "Repository CI, race, security qualification and packet-wide mutation testing complete with no lived/not-covered/timed-out mutants on changed critical semantics."
  ]
}
